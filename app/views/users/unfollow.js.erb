$('.follow-<%=@user.id%>').html('<%= link_to "Follow", follow_user_path(@user.id), remote: true, class: "btn btn-small" %>');

<% if current_user.follow_count == 0%>
  $('#following').css({
    display: "none"
  });
<%end%>

